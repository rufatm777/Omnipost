[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"
  included_files = ["data/**"]

# Redirect OAuth callbacks properly
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
